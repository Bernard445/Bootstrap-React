# 🧠 Bootstrap Responsive Webpage Assignment

## 📋 Overview
This project demonstrates a **fully responsive webpage** built using **Bootstrap 5**.  
It was created to practice key Bootstrap concepts including forms, tables, utilities, responsive images, and navigation bars.

The webpage includes:
- A responsive **registration form**
- A **Bootstrap table** displaying simulated user data
- A **responsive banner image** and **circular profile image**
- **Buttons** using Bootstrap utility classes
- A **navigation bar** with links that adjust to different screen sizes

---

## 🧱 Features
### 🧩 Form Creation and Layout
- Includes fields for **First Name, Last Name, Email, Password**, and a checkbox for agreeing to terms.  
- Uses Bootstrap’s **grid layout** (`col-md-6`) to align form fields side by side.  
- Includes validation with the `required` attribute.  
- Submit button styled with `btn-success`.

### 📊 Table for Displaying Data
- Uses the Bootstrap **table component** (`table`, `table-striped`, `table-hover`).  
- Displays **hard-coded sample data** for users.  
- Wrapped inside a `.table-responsive` div for mobile-friendly design.

### 🖼️ Image and Button Utilities
- Includes a responsive **banner image** using `img-fluid`.  
- Displays a **circular image** using `rounded-circle`.  
- Two buttons below the images:
  - One visible on all screen sizes.  
  - One hidden on small screens using `d-none d-md-block`.

### 🧭 Navigation Bar
- Built using `navbar`, `navbar-expand-lg`, and `navbar-light bg-light`.  
- Includes links to **Home, About, and Contact**.  
- Designed to collapse on smaller screens (mobile-friendly).

---

## 💻 Technologies Used
- **HTML5**
- **CSS3**
- **Bootstrap 5.3.2**

---

## 🧰 How to View
1. Clone or download this repository.
   ```bash
   git clone https://github.com/YourUsername/bootstrap-assignment
